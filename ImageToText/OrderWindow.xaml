<Window x:Class="ImageToText.OrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageToText"
        mc:Ignorable="d"
        Title="OrderWindow" Height="200" Width="150">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Name="LabelsStackPanel" />
        <StackPanel Grid.Row="0" Grid.Column="1" Name="OrdersStackPanel" />
        <Button Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Name="OkButton" Content="OK" Click="OkButton_Click" Background="LightBlue" />
    </Grid>
</Window>
